//! konfig-cli — operator CLI for the Konfig V0 gRPC service.
//!
//! Phase 1 scaffold — full implementation is Phase 2A (after the gRPC service
//! trait is defined in `konfig_service.fbs` and promoted to `rust/konfig`).

fn main() {
    todo!("konfig-cli: Phase 2A — implement get/apply subcommands once KonfigService gRPC is live")
}
